#ifndef PUSH_SWAP_H
# define PUSH_SWAP_H
# include <stdio.h>
# include <stdlib.h>
# include "stack.h"

t_node	*swap(t_stack *stack);
t_node	*rotate(t_stack *stack);
t_node	*revrotate(t_stack *stack);

#endif
